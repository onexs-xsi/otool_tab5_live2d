## IDF Component Manager Manifest File
description: otool_llm_sdk transport test app (runs against local_sse_server.py; works on the linux target and on esp32 targets with network)
version: "0.1.0"
dependencies:
  idf:
    version: ">=6.1.0-beta1,<6.2.0"
